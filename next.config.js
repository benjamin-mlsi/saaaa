/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Entfernen Sie diese Zeile, falls sie vorhanden ist, um dynamische Routen zu ermöglichen
  output: "export",
};

module.exports = nextConfig;
